# Placeholder (Documentation Unavailable)
def send_sms(message:str, destination:str, tries:int) -> bool:
    status = True


    # Send message
    print(f"Sending: \n{message}\n to {destination}")

    return status